# Lets Node resolve deps from the workspace root (fixes MODULE_NOT_FOUND for @nestjs/config
# when running `node dist/...` from apps/api). Re-run `pnpm install` after changing this.
node-linker=hoisted

public-hoist-pattern[]=*nestjs*
public-hoist-pattern[]=*prisma*
